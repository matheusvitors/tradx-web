const prefix = '@tradx';
export const KEY_TOKEN = `${prefix}:token`;
export const KEY_THEME = `${prefix}:theme`;
export const KEY_ATIVOS = `${prefix}:ativos`;
export const KEY_CONTAS = `${prefix}:contas`;
export const KEY_CONTA_SELECIONADA = `${prefix}:conta-selecionada`;
export const KEY_PERIODO_ATUAL = `${prefix}:periodo-atual`;
export const KEY_TABLE_VISIBILITY = `${prefix}:table-visibility`;

//TODO: Transformar em enum
